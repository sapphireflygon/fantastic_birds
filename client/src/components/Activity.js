// will contain the selected activity (will have each of our separate activities as children and will then only display whichever is selected)

import React from 'react';

const Activity = () => {
    return (
        <>

        </>
    )
};

export default Activity;